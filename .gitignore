# Production credentials, written by the deploy pipeline from GitHub secrets.
src/config.local.php

# Runtime artefacts
src/app.log
app.log

# FTP-Deploy-Action state (lives on the server, may get pulled down locally)
.ftp-deploy-sync-state.json

# One-shot schema appliers generated during a deploy run
_sg-apply-schema-*.php
src/_sg-apply-schema-*.php
